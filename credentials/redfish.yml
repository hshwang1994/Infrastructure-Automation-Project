---
# redfish target_type 자격증명 — vault/redfish.yml 의 평문 원본.
# 값 변경 시 ./scripts/encrypt-vault.sh 로 vault/redfish.yml 을 다시 만들어야 한다.
#
# 지금은 모든 BMC vendor (dell / hpe / lenovo / supermicro) 가 같은 계정을 쓴다고
# 가정하고 한 파일만 둔다. vendor 마다 계정이 달라지면 credentials/redfish-{vendor}.yml
# 로 쪼개고 playbook 쪽도 vendor 분기로 바꿔야 한다.

ansible_user:     infra
ansible_password: infra1234
