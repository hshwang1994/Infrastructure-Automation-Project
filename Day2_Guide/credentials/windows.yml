---
# windows target_type 자격증명 — vault/windows.yml 의 평문 원본.
# 값 변경 시 ./scripts/encrypt-vault.sh windows 로 vault/windows.yml 을 다시 만들어야 한다.
#
# 이 파일에는 계정/비번만 둔다. WinRM 의 transport / scheme / port 같은 비밀번호가 아닌
# 연결 설정은 vault 가 아니라 각 playbook 의 vars 블록에 두는 게 표준이다.
# (docs/playbook-guide.md 와 playbook/tasks/windows/*/site.yml 의 vars 블록 참고)

ansible_user:     infra
ansible_password: infra1234
